import java.util.*;
public class Solution {
    static boolean isKthBitSet(int n, int k) {
        // Check if the kth bit is set in number n
        if ((n & (1 << (k - 1))) != 0) {
            return true;
        }
        return false;
    }
}
