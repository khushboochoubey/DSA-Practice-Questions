public class Solution {
    public static int getSingleElement(int[] arr) {
        int n = arr.length;
        int res = 0;

        // XOR all elements to find the single element
        for (int i = 0; i < n; i++) {
            res ^= arr[i];
        }

        return res;
    }
}
